weather {
  routes {
    # If ask takes more time than this to complete the request is failed
    ask-timeout = 5s
  }
  openweather {
    # note that normally I probably would avoid hardcoding an API key in a config;
    # on Kubernetes, for example, I would use it as a secret loaded by the app at runtime
    api-key = "886705b4c1182eb1c69f28eb8c520e20"
  }
}
